# The main pipeline for databricks_classify_tickets
resources:
  pipelines:
    databricks_classify_tickets_pipeline:
      name: databricks_classify_tickets_pipeline
      catalog: quickstart_catalog_vkm_external
      schema: classify_tickets
      serverless: true
      libraries:
        - notebook:
            path: ../src/databricks_classify_tickets/validation_summary.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
